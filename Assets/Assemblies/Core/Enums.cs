using System;

namespace Larnix.Core
{
    public enum ParticleID : ushort
    {
        BlockBreak = 0,
        BlockPlace = 1,
    }

    public enum Weather : ushort
    {
        Clear = 0,
        Rain = 1,
        Snow = 2
    }
}
